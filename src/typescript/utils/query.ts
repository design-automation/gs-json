//function for querying the gs-JSON dataset

export function numPoints(model: GSJSON):number {
    //return number of points
    return 5;
}

export function numVertices(model: GSJSON):number {
    //return number of points
    return 5;
}